Write the complete `instances.yml` for one AI Tutor. This is a small filename-matching task.

COURSE
- Semester: {{semester}}
- Course ID: {{courseid}}
- Tutor ID: {{tutorid}}
- Tutor: {{tutor_label}}

BRIEF INSTRUCTION
{{tutor_guidance}}

DOCUMENT ROLES
The file types are allowed in the listed preferred order.
{{document_roles}}

EXAMPLE YAML
This example was generated from the Tutor's required document roles. Replace
its placeholder paths and add or remove instances as needed.
```yaml
{{example_yaml}}
```

FILES IN `materials/ps`
Paths below are relative to `materials/` and must be copied exactly.
{{ps_files}}


TEACHER'S TEXT
{{user_guidance}}

TASK
- Make a good guess from folder names, filename stems, and markers such as
  `solution`, `solutions`, `sol`, or `answer`.
- Use only files listed above. Do not read or convert files.
- Produce the complete YAML mapping with `course_docs` and `instances`.
- Use unique, short, stable `instanceid` values.
- Give every instance a concise, human-readable `label` for the Student App.
- Do not call tools. Do not discuss alternatives or provide a plan.
- Return exactly the following three parts and nothing else:
BEGIN_ULLME_INSTANCES_YAML
<the complete valid YAML mapping>
END_ULLME_INSTANCES_YAML
